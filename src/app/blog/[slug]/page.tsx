import { Header } from '@/components/Header'; import { Footer } from '@/components/Footer'; import { blogPosts } from '@/data/site'; import Link from 'next/link'; import { notFound } from 'next/navigation';
export function generateStaticParams(){return blogPosts.map(p=>({slug:p.slug}))}
export default function BlogDetail({params}:{params:{slug:string}}){const post=blogPosts.find(p=>p.slug===params.slug); if(!post)return notFound(); return <main className="bg-bg text-dark"><Header/><section className="bg-dark py-24 text-white"><div className="section-container"><Link href="/blog" className="text-sm font-bold text-cyan-200">← Quay lại Blog</Link><p className="mt-12 text-sm font-black uppercase tracking-[.18em] text-cyan-300">{post.category}</p><h1 className="mt-5 max-w-4xl text-5xl font-black md:text-7xl">{post.title}</h1><p className="mt-6 max-w-3xl text-xl leading-9 text-slate-300">{post.excerpt}</p></div></section><section className="py-20"><article className="section-container max-w-4xl rounded-[32px] border border-border bg-white p-8 shadow-sm"><div className="space-y-6 text-lg leading-9 text-slate-700">{post.body.map(p=><p key={p}>{p}</p>)}</div><div className="mt-10 rounded-2xl bg-bg p-5 text-sm font-bold text-primary">Bài viết thuộc hành trình Build In Public của Huy Automation System.</div></article></section><Footer/></main>}
